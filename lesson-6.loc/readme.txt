/* Структура сайта */

/lesson-7.loc
  |__/comments      // файлы комментариев (папка)
  |
  |__/css           // файлы стилей (папка)
  |
  |__/img           // картинки сайта (папка)
  |
  |__about.html     // файл странички обо мне. Обо мне
  |
  |__calc.php       // файл странички с калькулятором. Калькулятор
  |
  |__contacts.html  // файл странички с контактами. Контакты
  |
  |__data.php       // файл-обработчик с функциями, используемыми для страницы галерея и гостевая книга
  |
  |__gallery.php    // файл странички с выводом картинок в виде галереи. Галерея.
  |
  |__guestbook.php  // файл странички с выводом комментариев из папки /comments и формой их добавления. Гостевая книга
  |
  |__image.php      // файл для вывода пиктограмм картинок в полный размер
  |
  |__index.html     // файл главной страницы. Главная
  |
  |__news.html      // файл странички с новостями. Новости
  |
  |__readme.txt     // описание структуры сайта
  |
  |__upload.php     // файл-обработчик картинок от пользователя
  

/* Решение домашнего задания урока 7. */
1. Напишите калькулятор. Основывайтесь на коде, который был показан на уроке. Добавьте в форму выбор операции (сложение, вычитание, умножение, деление). Для создания выбора операции используйте элемент select ( http://htmlbook.ru/html/select ). В обработчике формы вам может быть полезен оператор switch...case ( http://php.net/manual/ru/control-structures.switch.php )
---
Решение этого задания с подробными комментариями находится в файле /calc.php
 
2. Переделайте свой скрипт гостевой книги. Добавьте в него форму добавления комментария и обработку данных из этой формы так, как было показано на уроке. Разберитесь с функцией file() самостоятельно ( http://php.net/manual/ru/function.file.php ), сделайте так, чтобы в конец строк из текстового файла не добавлялся символ переноса строки.
---
Решение этого задания с подробными комментариями находится в файле /guestbook.php

3. Доделайте свою фотогалерею из предыдущих уроков. Добавьте к ней форму загрузки нового файла и обработку загрузки.
4. * Подумайте - как можно ограничить размер заливаемых файлов? А их расширения (добавлять, например, только .jpg и .png)?
--
Форма с загрузкой файла от пользователя и программа обработки этого файла находятся в файле gallery.php. 
Более подробные комментарии уже в самом файле. 



 
  
  
/* Решение домашнего задания урока 6. */
Все функции, используемые для решения дз 6 урока, находятся в файле data.php

1. Напишите свою функцию, которая будет на вход получать путь до папки, 
а возвращать массив - список файлов из этой папки. В результате не должно быть файлов "." и ".."!
--
Помечено как выполнено.

2. Переработайте свою фотогалерею в соответствии с тем, что вам показывалось на уроке 
- страница галереи должна самостоятельно получать список фотографий из папки. Используйте свою функцию из п.1
--
Использовал функцию из п.1 и включил в новую функцию путь к папке с картинками галереи, чтобы код html галереи был чище.

function imagesList() {
    return filesList(__DIR__ . '/img/galleryBig');
}

Вы так делали на последней консультации. Чтобы данная функция вас не смущала, я её исключю.
Файлы: guestbook.php и data.php

3. Переделайте свою страницу комментариев. Пусть каждый уже имеющийся комментарий будет находиться в отдельном текстовом файле,
страница получит список таких файлов и прочитает каждый из них, вставляя на страницу. 
Используйте свою функцию из п.1 и функцию file_get_contents().
--
Используя функцию из п.1 получаю путь до папки с комментариями, и с помощью цикла foreach и функции file_get_contents вывожу комментарии на страницу.
Файл: guestbook.php


4. * Напишите функцию getAllComments(), которая на вход получит папку, в которой находятся файлы комментариев (см. п. 3), 
а вернет массив, в котором ключи - это время, когда был оставлен комментарий (подсказка - сохраняйте это время в имени файла),
а значения - тексты комментариев. Примените функцию для вывода комментариев на странице из пункта 3.
--
Написал функцию getAllComments(), которая на вход получила папку, в которой находятся файлы комментариев, 
а вернула массив, в котором ключи - это время, когда был оставлен комментарий, а значения - тексты комментариев.
Файл: data.php

Вывел полученный массив с помощью цикла foreach, добавив в комментарии время, которое содержится в ключах массива.
Файл: guestbook.php


